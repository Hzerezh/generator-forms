// Этот файл используется для глобальной настройки Vitest
import { beforeAll } from 'vitest';

// Пример глобальной настройки (если требуется)
beforeAll(() => {
  // Глобальная инициализация для тестов
});